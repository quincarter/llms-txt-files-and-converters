# Seerr API
Description: This is the documentation for the Seerr API backend.

Two primary authentication methods are supported:

- **Cookie Authentication**: A valid sign-in to the `/auth/plex` or `/auth/local` will generate a valid authentication cookie.
- **API Key Authentication**: Sign-in is also possible by passing an `X-Api-Key` header along with a valid API Key generated by Seerr.


## GET /status
**Summary**: Get Seerr status
**Details**: Returns the current Seerr status in a JSON object.

## GET /status/appdata
**Summary**: Get application data volume status
**Details**: For Docker installs, returns whether or not the volume mount was configured properly. Always returns true for non-Docker installs.

## GET /settings/main
**Summary**: Get main settings
**Details**: Retrieves all main settings in a JSON object.

## POST /settings/main
**Summary**: Update main settings
**Details**: Updates main settings with the provided values.

## GET /settings/network
**Summary**: Get network settings
**Details**: Retrieves all network settings in a JSON object.

## POST /settings/network
**Summary**: Update network settings
**Details**: Updates network settings with the provided values.

## POST /settings/main/regenerate
**Summary**: Get main settings with newly-generated API key
**Details**: Returns main settings in a JSON object, using the new API key.

## GET /settings/jellyfin
**Summary**: Get Jellyfin settings
**Details**: Retrieves current Jellyfin settings.

## POST /settings/jellyfin
**Summary**: Update Jellyfin settings
**Details**: Updates Jellyfin settings with the provided values.

## GET /settings/jellyfin/library
**Summary**: Get Jellyfin libraries
**Details**: Returns a list of Jellyfin libraries in a JSON array.
**Params**: sync (query), enable (query)

## GET /settings/jellyfin/users
**Summary**: Get Jellyfin Users
**Details**: Returns a list of Jellyfin Users in a JSON array.

## GET /settings/jellyfin/sync
**Summary**: Get status of full Jellyfin library sync
**Details**: Returns sync progress in a JSON array.

## POST /settings/jellyfin/sync
**Summary**: Start full Jellyfin library sync
**Details**: Runs a full Jellyfin library sync and returns the progress in a JSON array.

## GET /settings/plex
**Summary**: Get Plex settings
**Details**: Retrieves current Plex settings.

## POST /settings/plex
**Summary**: Update Plex settings
**Details**: Updates Plex settings with the provided values.

## GET /settings/plex/library
**Summary**: Get Plex libraries
**Details**: Returns a list of Plex libraries in a JSON array.
**Params**: sync (query), enable (query)

## GET /settings/plex/sync
**Summary**: Get status of full Plex library scan
**Details**: Returns scan progress in a JSON array.

## POST /settings/plex/sync
**Summary**: Start full Plex library scan
**Details**: Runs a full Plex library scan and returns the progress in a JSON array.

## GET /settings/plex/devices/servers
**Summary**: Gets the user's available Plex servers
**Details**: Returns a list of available Plex servers and their connectivity state

## GET /settings/plex/users
**Summary**: Get Plex users
**Details**: Returns a list of Plex users in a JSON array.

## GET /settings/metadatas
**Summary**: Get Metadata settings
**Details**: Retrieves current Metadata settings.

## PUT /settings/metadatas
**Summary**: Update Metadata settings
**Details**: Updates Metadata settings with the provided values.

## POST /settings/metadatas/test
**Summary**: Test Provider configuration
**Details**: Tests if the TVDB configuration is valid. Returns a list of available languages on success.

## GET /settings/tautulli
**Summary**: Get Tautulli settings
**Details**: Retrieves current Tautulli settings.

## POST /settings/tautulli
**Summary**: Update Tautulli settings
**Details**: Updates Tautulli settings with the provided values.

## GET /settings/radarr
**Summary**: Get Radarr settings
**Details**: Returns all Radarr settings in a JSON array.

## POST /settings/radarr
**Summary**: Create Radarr instance
**Details**: Creates a new Radarr instance from the request body.

## POST /settings/radarr/test
**Summary**: Test Radarr configuration
**Details**: Tests if the Radarr configuration is valid. Returns profiles and root folders on success.

## PUT /settings/radarr/{radarrId}
**Summary**: Update Radarr instance
**Details**: Updates an existing Radarr instance with the provided values.
**Params**: radarrId* (path)

## DELETE /settings/radarr/{radarrId}
**Summary**: Delete Radarr instance
**Details**: Deletes an existing Radarr instance based on the radarrId parameter.
**Params**: radarrId* (path)

## GET /settings/radarr/{radarrId}/profiles
**Summary**: Get available Radarr profiles
**Details**: Returns a list of profiles available on the Radarr server instance in a JSON array.
**Params**: radarrId* (path)

## GET /settings/sonarr
**Summary**: Get Sonarr settings
**Details**: Returns all Sonarr settings in a JSON array.

## POST /settings/sonarr
**Summary**: Create Sonarr instance
**Details**: Creates a new Sonarr instance from the request body.

## POST /settings/sonarr/test
**Summary**: Test Sonarr configuration
**Details**: Tests if the Sonarr configuration is valid. Returns profiles and root folders on success.

## PUT /settings/sonarr/{sonarrId}
**Summary**: Update Sonarr instance
**Details**: Updates an existing Sonarr instance with the provided values.
**Params**: sonarrId* (path)

## DELETE /settings/sonarr/{sonarrId}
**Summary**: Delete Sonarr instance
**Details**: Deletes an existing Sonarr instance based on the sonarrId parameter.
**Params**: sonarrId* (path)

## GET /settings/public
**Summary**: Get public settings
**Details**: Returns settings that are not protected or sensitive. Mainly used to determine if the application has been configured for the first time.

## POST /settings/initialize
**Summary**: Initialize application
**Details**: Sets the app as initialized, allowing the user to navigate to pages other than the setup page.

## GET /settings/jobs
**Summary**: Get scheduled jobs
**Details**: Returns list of all scheduled jobs and details about their next execution time in a JSON array.

## POST /settings/jobs/{jobId}/run
**Summary**: Invoke a specific job
**Details**: Invokes a specific job to run. Will return the new job status in JSON format.
**Params**: jobId* (path)

## POST /settings/jobs/{jobId}/cancel
**Summary**: Cancel a specific job
**Details**: Cancels a specific job. Will return the new job status in JSON format.
**Params**: jobId* (path)

## POST /settings/jobs/{jobId}/schedule
**Summary**: Modify job schedule
**Details**: Re-registers the job with the schedule specified. Will return the job in JSON format.
**Params**: jobId* (path)

## GET /settings/cache
**Summary**: Get a list of active caches
**Details**: Retrieves a list of all active caches and their current stats.

## POST /settings/cache/{cacheId}/flush
**Summary**: Flush a specific cache
**Details**: Flushes all data from the cache ID provided
**Params**: cacheId* (path)

## POST /settings/cache/dns/{dnsEntry}/flush
**Summary**: Flush a specific DNS cache entry
**Details**: Flushes a specific DNS cache entry
**Params**: dnsEntry* (path)

## GET /settings/logs
**Summary**: Returns logs
**Details**: Returns list of all log items and details
**Params**: take (query), skip (query), filter (query), search (query)

## GET /settings/notifications/email
**Summary**: Get email notification settings
**Details**: Returns current email notification settings in a JSON object.

## POST /settings/notifications/email
**Summary**: Update email notification settings
**Details**: Updates email notification settings with provided values

## POST /settings/notifications/email/test
**Summary**: Test email settings
**Details**: Sends a test notification to the email agent.

## GET /settings/notifications/discord
**Summary**: Get Discord notification settings
**Details**: Returns current Discord notification settings in a JSON object.

## POST /settings/notifications/discord
**Summary**: Update Discord notification settings
**Details**: Updates Discord notification settings with the provided values.

## POST /settings/notifications/discord/test
**Summary**: Test Discord settings
**Details**: Sends a test notification to the Discord agent.

## GET /settings/notifications/pushbullet
**Summary**: Get Pushbullet notification settings
**Details**: Returns current Pushbullet notification settings in a JSON object.

## POST /settings/notifications/pushbullet
**Summary**: Update Pushbullet notification settings
**Details**: Update Pushbullet notification settings with the provided values.

## POST /settings/notifications/pushbullet/test
**Summary**: Test Pushbullet settings
**Details**: Sends a test notification to the Pushbullet agent.

## GET /settings/notifications/pushover
**Summary**: Get Pushover notification settings
**Details**: Returns current Pushover notification settings in a JSON object.

## POST /settings/notifications/pushover
**Summary**: Update Pushover notification settings
**Details**: Update Pushover notification settings with the provided values.

## POST /settings/notifications/pushover/test
**Summary**: Test Pushover settings
**Details**: Sends a test notification to the Pushover agent.

## GET /settings/notifications/pushover/sounds
**Summary**: Get Pushover sounds
**Details**: Returns valid Pushover sound options in a JSON array.
**Params**: token* (query)

## GET /settings/notifications/gotify
**Summary**: Get Gotify notification settings
**Details**: Returns current Gotify notification settings in a JSON object.

## POST /settings/notifications/gotify
**Summary**: Update Gotify notification settings
**Details**: Update Gotify notification settings with the provided values.

## POST /settings/notifications/gotify/test
**Summary**: Test Gotify settings
**Details**: Sends a test notification to the Gotify agent.

## GET /settings/notifications/ntfy
**Summary**: Get ntfy.sh notification settings
**Details**: Returns current ntfy.sh notification settings in a JSON object.

## POST /settings/notifications/ntfy
**Summary**: Update ntfy.sh notification settings
**Details**: Update ntfy.sh notification settings with the provided values.

## POST /settings/notifications/ntfy/test
**Summary**: Test ntfy.sh settings
**Details**: Sends a test notification to the ntfy.sh agent.

## GET /settings/notifications/slack
**Summary**: Get Slack notification settings
**Details**: Returns current Slack notification settings in a JSON object.

## POST /settings/notifications/slack
**Summary**: Update Slack notification settings
**Details**: Updates Slack notification settings with the provided values.

## POST /settings/notifications/slack/test
**Summary**: Test Slack settings
**Details**: Sends a test notification to the Slack agent.

## GET /settings/notifications/telegram
**Summary**: Get Telegram notification settings
**Details**: Returns current Telegram notification settings in a JSON object.

## POST /settings/notifications/telegram
**Summary**: Update Telegram notification settings
**Details**: Update Telegram notification settings with the provided values.

## POST /settings/notifications/telegram/test
**Summary**: Test Telegram settings
**Details**: Sends a test notification to the Telegram agent.

## GET /settings/notifications/webpush
**Summary**: Get Web Push notification settings
**Details**: Returns current Web Push notification settings in a JSON object.

## POST /settings/notifications/webpush
**Summary**: Update Web Push notification settings
**Details**: Updates Web Push notification settings with the provided values.

## POST /settings/notifications/webpush/test
**Summary**: Test Web Push settings
**Details**: Sends a test notification to the Web Push agent.

## GET /settings/notifications/webhook
**Summary**: Get webhook notification settings
**Details**: Returns current webhook notification settings in a JSON object.

## POST /settings/notifications/webhook
**Summary**: Update webhook notification settings
**Details**: Updates webhook notification settings with the provided values.

## POST /settings/notifications/webhook/test
**Summary**: Test webhook settings
**Details**: Sends a test notification to the webhook agent.

## GET /settings/discover
**Summary**: Get all discover sliders
**Details**: Returns all discovery sliders. Built-in and custom made.

## POST /settings/discover
**Summary**: Batch update all sliders.
**Details**: Batch update all sliders at once. Should also be used for creation. Will only update sliders provided

## PUT /settings/discover/{sliderId}
**Summary**: Update a single slider
**Details**: Updates a single slider and return the newly updated slider. Requires the `ADMIN` permission.
**Params**: sliderId* (path)

## DELETE /settings/discover/{sliderId}
**Summary**: Delete slider by ID
**Details**: Deletes the slider with the provided sliderId. Requires the `ADMIN` permission.
**Params**: sliderId* (path)

## POST /settings/discover/add
**Summary**: Add a new slider
**Details**: Add a single slider and return the newly created slider. Requires the `ADMIN` permission.

## GET /settings/discover/reset
**Summary**: Reset all discover sliders
**Details**: Resets all discovery sliders to the default values. Requires the `ADMIN` permission.

## GET /settings/about
**Summary**: Get server stats
**Details**: Returns current server stats in a JSON object.

## GET /auth/me
**Summary**: Get logged-in user
**Details**: Returns the currently logged-in user.

## POST /auth/plex
**Summary**: Sign in using a Plex token
**Details**: Takes an `authToken` (Plex token) to log the user in. Generates a session cookie for use in further requests. If the user does not exist, and there are no other users, then a user will be created with

## POST /auth/jellyfin
**Summary**: Sign in using a Jellyfin username and password
**Details**: Takes the user's username and password to log the user in. Generates a session cookie for use in further requests. If the user does not exist, and there are no other users, then a user will be created

## POST /auth/local
**Summary**: Sign in using a local account
**Details**: Takes an `email` and a `password` to log the user in. Generates a session cookie for use in further requests.

## POST /auth/logout
**Summary**: Sign out and clear session cookie
**Details**: Completely clear the session cookie and associated values, effectively signing the user out.

## POST /auth/reset-password
**Summary**: Send a reset password email
**Details**: Sends a reset password email to the email if the user exists

## POST /auth/reset-password/{guid}
**Summary**: Reset the password for a user
**Details**: Resets the password for a user if the given guid is connected to a user
**Params**: guid* (path)

## GET /user
**Summary**: Get all users
**Details**: Returns all users in a JSON object.
**Params**: take (query), skip (query), sort (query), q (query), includeIds (query)

## POST /user
**Summary**: Create new user
**Details**: Creates a new user. Requires the `MANAGE_USERS` permission.

## PUT /user
**Summary**: Update batch of users
**Details**: Update users with given IDs with provided values in request `body.settings`. You cannot update users' Plex tokens through this request.

## POST /user/import-from-plex
**Summary**: Import all users from Plex
**Details**: Fetches and imports users from the Plex server. If a list of Plex IDs is provided in the request body, only the specified users will be imported. Otherwise, all users will be imported.

## POST /user/import-from-jellyfin
**Summary**: Import all users from Jellyfin
**Details**: Fetches and imports users from the Jellyfin server.

## POST /user/registerPushSubscription
**Summary**: Register a web push /user/registerPushSubscription
**Details**: Registers a web push subscription for the logged-in user

## GET /user/{userId}/pushSubscriptions
**Summary**: Get all web push notification settings for a user
**Details**: Returns all web push notification settings for a user in a JSON object.
**Params**: userId* (path)

## GET /user/{userId}/pushSubscription/{endpoint}
**Summary**: Get web push notification settings for a user
**Details**: Returns web push notification settings for a user in a JSON object.
**Params**: userId* (path), endpoint* (path)

## DELETE /user/{userId}/pushSubscription/{endpoint}
**Summary**: Delete user push subscription by key
**Details**: Deletes the user push subscription with the provided key.
**Params**: userId* (path), endpoint* (path)

## GET /user/{userId}
**Summary**: Get user by ID
**Details**: Retrieves user details in a JSON object. Requires the `MANAGE_USERS` permission.
**Params**: userId* (path)

## PUT /user/{userId}
**Summary**: Update a user by user ID
**Details**: Update a user with the provided values. You cannot update a user's Plex token through this request.
**Params**: userId* (path)

## DELETE /user/{userId}
**Summary**: Delete user by ID
**Details**: Deletes the user with the provided userId. Requires the `MANAGE_USERS` permission.
**Params**: userId* (path)

## GET /user/{userId}/requests
**Summary**: Get requests for a specific user
**Details**: Retrieves a user's requests in a JSON object.
**Params**: userId* (path), take (query), skip (query)

## GET /user/{userId}/quota
**Summary**: Get quotas for a specific user
**Details**: Returns quota details for a user in a JSON object. Requires `MANAGE_USERS` permission if viewing other users.
**Params**: userId* (path)

## GET /blocklist
**Summary**: Returns blocklisted items
**Details**: Returns list of all blocklisted media
**Params**: take (query), skip (query), search (query), filter (query)

## POST /blocklist
**Summary**: Add media to blocklist

## GET /blocklist/{tmdbId}
**Summary**: Get media from blocklist
**Params**: tmdbId* (path)

## DELETE /blocklist/{tmdbId}
**Summary**: Remove media from blocklist
**Params**: tmdbId* (path)

## GET /blacklist
**Summary**: Returns blocklisted items
**Details**: **DEPRECATED**: Use `/blocklist` instead. This endpoint will be deprecated soon.
**Params**: take (query), skip (query), search (query), filter (query)

## POST /blacklist
**Summary**: Add media to blocklist
**Details**: **DEPRECATED**: Use `/blocklist` instead. This endpoint will be deprecated soon.

## GET /blacklist/{tmdbId}
**Summary**: Get media from blocklist
**Details**: **DEPRECATED**: Use `/blocklist/{tmdbId}` instead. This endpoint will be deprecated soon.
**Params**: tmdbId* (path)

## DELETE /blacklist/{tmdbId}
**Summary**: Remove media from blocklist
**Details**: **DEPRECATED**: Use `/blocklist/{tmdbId}` instead. This endpoint will be deprecated soon.
**Params**: tmdbId* (path)

## POST /watchlist
**Summary**: Add media to watchlist

## DELETE /watchlist/{tmdbId}
**Summary**: Delete watchlist item
**Details**: Removes a watchlist item.
**Params**: tmdbId* (path)

## GET /user/{userId}/watchlist
**Summary**: Get the Plex watchlist for a specific user
**Details**: Retrieves a user's Plex Watchlist in a JSON object.
**Params**: userId* (path), page (query)

## GET /user/{userId}/settings/main
**Summary**: Get general settings for a user
**Details**: Returns general settings for a specific user. Requires `MANAGE_USERS` permission if viewing other users.
**Params**: userId* (path)

## POST /user/{userId}/settings/main
**Summary**: Update general settings for a user
**Details**: Updates and returns general settings for a specific user. Requires `MANAGE_USERS` permission if editing other users.
**Params**: userId* (path)

## GET /user/{userId}/settings/password
**Summary**: Get password page informatiom
**Details**: Returns important data for the password page to function correctly. Requires `MANAGE_USERS` permission if viewing other users.
**Params**: userId* (path)

## POST /user/{userId}/settings/password
**Summary**: Update password for a user
**Details**: Updates a user's password. Requires `MANAGE_USERS` permission if editing other users.
**Params**: userId* (path)

## POST /user/{userId}/settings/linked-accounts/plex
**Summary**: Link the provided Plex account to the current user
**Details**: Logs in to Plex with the provided auth token, then links the associated Plex account with the user's account. Users can only link external accounts to their own account.
**Params**: userId* (path)

## DELETE /user/{userId}/settings/linked-accounts/plex
**Summary**: Remove the linked Plex account for a user
**Details**: Removes the linked Plex account for a specific user. Requires `MANAGE_USERS` permission if editing other users.
**Params**: userId* (path)

## POST /user/{userId}/settings/linked-accounts/jellyfin
**Summary**: Link the provided Jellyfin account to the current user
**Details**: Logs in to Jellyfin with the provided credentials, then links the associated Jellyfin account with the user's account. Users can only link external accounts to their own account.
**Params**: userId* (path)

## DELETE /user/{userId}/settings/linked-accounts/jellyfin
**Summary**: Remove the linked Jellyfin account for a user
**Details**: Removes the linked Jellyfin account for a specific user. Requires `MANAGE_USERS` permission if editing other users.
**Params**: userId* (path)

## GET /user/{userId}/settings/notifications
**Summary**: Get notification settings for a user
**Details**: Returns notification settings for a specific user. Requires `MANAGE_USERS` permission if viewing other users.
**Params**: userId* (path)

## POST /user/{userId}/settings/notifications
**Summary**: Update notification settings for a user
**Details**: Updates and returns notification settings for a specific user. Requires `MANAGE_USERS` permission if editing other users.
**Params**: userId* (path)

## GET /user/{userId}/settings/permissions
**Summary**: Get permission settings for a user
**Details**: Returns permission settings for a specific user. Requires `MANAGE_USERS` permission if viewing other users.
**Params**: userId* (path)

## POST /user/{userId}/settings/permissions
**Summary**: Update permission settings for a user
**Details**: Updates and returns permission settings for a specific user. Requires `MANAGE_USERS` permission if editing other users.
**Params**: userId* (path)

## GET /user/{userId}/watch_data
**Summary**: Get watch data
**Details**: Returns play count, play duration, and recently watched media.
**Params**: userId* (path)

## GET /search
**Summary**: Search for movies, TV shows, or people
**Details**: Returns a list of movies, TV shows, or people a JSON object.
**Params**: query* (query), page (query), language (query)

## GET /search/keyword
**Summary**: Search for keywords
**Details**: Returns a list of TMDB keywords matching the search query
**Params**: query* (query), page (query)

## GET /search/company
**Summary**: Search for companies
**Details**: Returns a list of TMDB companies matching the search query. (Will not return origin country)
**Params**: query* (query), page (query)

## GET /discover/movies
**Summary**: Discover movies
**Details**: Returns a list of movies in a JSON object.
**Params**: page (query), language (query), genre (query), studio (query), keywords (query), excludeKeywords (query), sortBy (query), primaryReleaseDateGte (query), primaryReleaseDateLte (query), withRuntimeGte (query), withRuntimeLte (query), voteAverageGte (query), voteAverageLte (query), voteCountGte (query), voteCountLte (query), watchRegion (query), watchProviders (query), certification (query), certificationGte (query), certificationLte (query), certificationCountry (query), certificationMode (query)

## GET /discover/movies/genre/{genreId}
**Summary**: Discover movies by genre
**Details**: Returns a list of movies based on the provided genre ID in a JSON object.
**Params**: genreId* (path), page (query), language (query)

## GET /discover/movies/language/{language}
**Summary**: Discover movies by original language
**Details**: Returns a list of movies based on the provided ISO 639-1 language code in a JSON object.
**Params**: language* (path), page (query), language (query)

## GET /discover/movies/studio/{studioId}
**Summary**: Discover movies by studio
**Details**: Returns a list of movies based on the provided studio ID in a JSON object.
**Params**: studioId* (path), page (query), language (query)

## GET /discover/movies/upcoming
**Summary**: Upcoming movies
**Details**: Returns a list of movies in a JSON object.
**Params**: page (query), language (query)

## GET /discover/tv
**Summary**: Discover TV shows
**Details**: Returns a list of TV shows in a JSON object.
**Params**: page (query), language (query), genre (query), network (query), keywords (query), excludeKeywords (query), sortBy (query), firstAirDateGte (query), firstAirDateLte (query), withRuntimeGte (query), withRuntimeLte (query), voteAverageGte (query), voteAverageLte (query), voteCountGte (query), voteCountLte (query), watchRegion (query), watchProviders (query), status (query), certification (query), certificationGte (query), certificationLte (query), certificationCountry (query), certificationMode (query)

## GET /discover/tv/language/{language}
**Summary**: Discover TV shows by original language
**Details**: Returns a list of TV shows based on the provided ISO 639-1 language code in a JSON object.
**Params**: language* (path), page (query), language (query)

## GET /discover/tv/genre/{genreId}
**Summary**: Discover TV shows by genre
**Details**: Returns a list of TV shows based on the provided genre ID in a JSON object.
**Params**: genreId* (path), page (query), language (query)

## GET /discover/tv/network/{networkId}
**Summary**: Discover TV shows by network
**Details**: Returns a list of TV shows based on the provided network ID in a JSON object.
**Params**: networkId* (path), page (query), language (query)

## GET /discover/tv/upcoming
**Summary**: Discover Upcoming TV shows
**Details**: Returns a list of upcoming TV shows in a JSON object.
**Params**: page (query), language (query)

## GET /discover/trending
**Summary**: Trending movies and TV
**Details**: Returns a list of movies and TV shows in a JSON object.
**Params**: page (query), language (query)

## GET /discover/keyword/{keywordId}/movies
**Summary**: Get movies from keyword
**Details**: Returns list of movies based on the provided keyword ID a JSON object.
**Params**: keywordId* (path), page (query), language (query)

## GET /discover/genreslider/movie
**Summary**: Get genre slider data for movies
**Details**: Returns a list of genres with backdrops attached
**Params**: language (query)

## GET /discover/genreslider/tv
**Summary**: Get genre slider data for TV series
**Details**: Returns a list of genres with backdrops attached
**Params**: language (query)

## GET /discover/watchlist
**Summary**: Get the Plex watchlist.
**Params**: page (query)

## GET /request
**Summary**: Get all requests
**Details**: Returns all requests if the user has the `ADMIN` or `MANAGE_REQUESTS` permissions. Otherwise, only the logged-in user's requests are returned.
**Params**: take (query), skip (query), filter (query), sort (query), sortDirection (query), requestedBy (query), mediaType (query)

## POST /request
**Summary**: Create new request
**Details**: Creates a new request with the provided media ID and type. The `REQUEST` permission is required.

## GET /request/count
**Summary**: Gets request counts
**Details**: Returns the number of requests by status including pending, approved, available, and completed requests.

## GET /request/{requestId}
**Summary**: Get MediaRequest
**Details**: Returns a specific MediaRequest in a JSON object.
**Params**: requestId* (path)

## PUT /request/{requestId}
**Summary**: Update MediaRequest
**Details**: Updates a specific media request and returns the request in a JSON object. Requires the `MANAGE_REQUESTS` permission.
**Params**: requestId* (path)

## DELETE /request/{requestId}
**Summary**: Delete request
**Details**: Removes a request. If the user has the `MANAGE_REQUESTS` permission, any request can be removed. Otherwise, only pending requests can be removed.
**Params**: requestId* (path)

## POST /request/{requestId}/retry
**Summary**: Retry failed request
**Details**: Retries a request by resending requests to Sonarr or Radarr.
**Params**: requestId* (path)

## POST /request/{requestId}/{status}
**Summary**: Update a request's status
**Details**: Updates a request's status to approved or declined. Also returns the request in a JSON object.
**Params**: requestId* (path), status* (path)

## GET /movie/{movieId}
**Summary**: Get movie details
**Details**: Returns full movie details in a JSON object.
**Params**: movieId* (path), language (query)

## GET /movie/{movieId}/recommendations
**Summary**: Get recommended movies
**Details**: Returns list of recommended movies based on provided movie ID in a JSON object.
**Params**: movieId* (path), page (query), language (query)

## GET /movie/{movieId}/similar
**Summary**: Get similar movies
**Details**: Returns list of similar movies based on the provided movieId in a JSON object.
**Params**: movieId* (path), page (query), language (query)

## GET /movie/{movieId}/ratings
**Summary**: Get movie ratings
**Details**: Returns ratings based on the provided movieId in a JSON object.
**Params**: movieId* (path)

## GET /movie/{movieId}/ratingscombined
**Summary**: Get RT and IMDB movie ratings combined
**Details**: Returns ratings from RottenTomatoes and IMDB based on the provided movieId in a JSON object.
**Params**: movieId* (path)

## GET /tv/{tvId}
**Summary**: Get TV details
**Details**: Returns full TV details in a JSON object.
**Params**: tvId* (path), language (query)

## GET /tv/{tvId}/season/{seasonNumber}
**Summary**: Get season details and episode list
**Details**: Returns season details with a list of episodes in a JSON object.
**Params**: tvId* (path), seasonNumber* (path), language (query)

## GET /tv/{tvId}/recommendations
**Summary**: Get recommended TV series
**Details**: Returns list of recommended TV series based on the provided tvId in a JSON object.
**Params**: tvId* (path), page (query), language (query)

## GET /tv/{tvId}/similar
**Summary**: Get similar TV series
**Details**: Returns list of similar TV series based on the provided tvId in a JSON object.
**Params**: tvId* (path), page (query), language (query)

## GET /tv/{tvId}/ratings
**Summary**: Get TV ratings
**Details**: Returns ratings based on provided tvId in a JSON object.
**Params**: tvId* (path)

## GET /person/{personId}
**Summary**: Get person details
**Details**: Returns person details based on provided personId in a JSON object.
**Params**: personId* (path), language (query)

## GET /person/{personId}/combined_credits
**Summary**: Get combined credits
**Details**: Returns the person's combined credits based on the provided personId in a JSON object.
**Params**: personId* (path), language (query)

## GET /media
**Summary**: Get media
**Details**: Returns all media (can be filtered and limited) in a JSON object.
**Params**: take (query), skip (query), filter (query), sort (query)

## DELETE /media/{mediaId}
**Summary**: Delete media item
**Details**: Removes a media item. The `MANAGE_REQUESTS` permission is required to perform this action.
**Params**: mediaId* (path)

## DELETE /media/{mediaId}/file
**Summary**: Delete media file
**Details**: Removes a media file from radarr/sonarr. The `ADMIN` permission is required to perform this action.
**Params**: mediaId* (path), is4k (query)

## POST /media/{mediaId}/{status}
**Summary**: Update media status
**Details**: Updates a media item's status and returns the media in JSON format
**Params**: mediaId* (path), status* (path)

## GET /media/{mediaId}/watch_data
**Summary**: Get watch data
**Details**: Returns play count, play duration, and users who have watched the media.
**Params**: mediaId* (path)

## GET /collection/{collectionId}
**Summary**: Get collection details
**Details**: Returns full collection details in a JSON object.
**Params**: collectionId* (path), language (query)

## GET /service/radarr
**Summary**: Get non-sensitive Radarr server list
**Details**: Returns a list of Radarr server IDs and names in a JSON object.

## GET /service/radarr/{radarrId}
**Summary**: Get Radarr server quality profiles and root folders
**Details**: Returns a Radarr server's quality profile and root folder details in a JSON object.
**Params**: radarrId* (path)

## GET /service/sonarr
**Summary**: Get non-sensitive Sonarr server list
**Details**: Returns a list of Sonarr server IDs and names in a JSON object.

## GET /service/sonarr/{sonarrId}
**Summary**: Get Sonarr server quality profiles and root folders
**Details**: Returns a Sonarr server's quality profile and root folder details in a JSON object.
**Params**: sonarrId* (path)

## GET /service/sonarr/lookup/{tmdbId}
**Summary**: Get series from Sonarr
**Details**: Returns a list of series returned by searching for the name in Sonarr.
**Params**: tmdbId* (path)

## GET /regions
**Summary**: Regions supported by TMDB
**Details**: Returns a list of regions in a JSON object.

## GET /languages
**Summary**: Languages supported by TMDB
**Details**: Returns a list of languages in a JSON object.

## GET /studio/{studioId}
**Summary**: Get movie studio details
**Details**: Returns movie studio details in a JSON object.
**Params**: studioId* (path)

## GET /network/{networkId}
**Summary**: Get TV network details
**Details**: Returns TV network details in a JSON object.
**Params**: networkId* (path)

## GET /genres/movie
**Summary**: Get list of official TMDB movie genres
**Details**: Returns a list of genres in a JSON array.
**Params**: language (query)

## GET /genres/tv
**Summary**: Get list of official TMDB movie genres
**Details**: Returns a list of genres in a JSON array.
**Params**: language (query)

## GET /backdrops
**Summary**: Get backdrops of trending items
**Details**: Returns a list of backdrop image paths in a JSON array.

## GET /issue
**Summary**: Get all issues
**Details**: Returns a list of issues in JSON format.
**Params**: take (query), skip (query), sort (query), filter (query), requestedBy (query)

## POST /issue
**Summary**: Create new issue
**Details**: Creates a new issue

## GET /issue/count
**Summary**: Gets issue counts
**Details**: Returns the number of open and closed issues, as well as the number of issues of each type.

## GET /issue/{issueId}
**Summary**: Get issue
**Details**: Returns a single issue in JSON format.
**Params**: issueId* (path)

## DELETE /issue/{issueId}
**Summary**: Delete issue
**Details**: Removes an issue. If the user has the `MANAGE_ISSUES` permission, any issue can be removed. Otherwise, only a users own issues can be removed.
**Params**: issueId* (path)

## POST /issue/{issueId}/comment
**Summary**: Create a comment
**Details**: Creates a comment and returns associated issue in JSON format.
**Params**: issueId* (path)

## GET /issueComment/{commentId}
**Summary**: Get issue comment
**Details**: Returns a single issue comment in JSON format.
**Params**: commentId* (path)

## PUT /issueComment/{commentId}
**Summary**: Update issue comment
**Details**: Updates and returns a single issue comment in JSON format.
**Params**: commentId* (path)

## DELETE /issueComment/{commentId}
**Summary**: Delete issue comment
**Details**: Deletes an issue comment. Only users with `MANAGE_ISSUES` or the user who created the comment can perform this action.
**Params**: commentId* (path)

## POST /issue/{issueId}/{status}
**Summary**: Update an issue's status
**Details**: Updates an issue's status to approved or declined. Also returns the issue in a JSON object.
**Params**: issueId* (path), status* (path)

## GET /keyword/{keywordId}
**Summary**: Get keyword
**Details**: Returns a single keyword in JSON format.
**Params**: keywordId* (path)

## GET /watchproviders/regions
**Summary**: Get watch provider regions
**Details**: Returns a list of all available watch provider regions.

## GET /watchproviders/movies
**Summary**: Get watch provider movies
**Details**: Returns a list of all available watch providers for movies.
**Params**: watchRegion* (query)

## GET /watchproviders/tv
**Summary**: Get watch provider series
**Details**: Returns a list of all available watch providers for series.
**Params**: watchRegion* (query)

## GET /certifications/movie
**Summary**: Get movie certifications
**Details**: Returns list of movie certifications from TMDB.

## GET /certifications/tv
**Summary**: Get TV certifications
**Details**: Returns list of TV show certifications from TMDB.

## GET /overrideRule
**Summary**: Get override rules
**Details**: Returns a list of all override rules with their conditions and settings

## POST /overrideRule
**Summary**: Create override rule
**Details**: Creates a new Override Rule from the request body.

## PUT /overrideRule/{ruleId}
**Summary**: Update override rule
**Details**: Updates an Override Rule from the request body.
**Params**: ruleId* (path)

## DELETE /overrideRule/{ruleId}
**Summary**: Delete override rule by ID
**Details**: Deletes the override rule with the provided ruleId.
**Params**: ruleId* (path)
